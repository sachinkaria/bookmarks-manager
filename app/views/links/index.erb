<h1>Welcome <%= @user.name %> </h1>
<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url %>
      Tag: <%= link.tag.map(&:name) %>
    </li>
  <% end %>
</ul>